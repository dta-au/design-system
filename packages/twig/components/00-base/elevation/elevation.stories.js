import Component from './elevation.stories.twig';

const meta = {
  title: 'Base/Elevation',
  component: Component,
  tags: ['!autodocs'],
};

export default meta;

export const Elevation = {
  parameters: {
    layout: 'centered',
  },
};
