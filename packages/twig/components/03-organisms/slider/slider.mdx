{/* Editorial body is shared with the design library – edit ./slider.md, not this file. */}

import { Meta, Canvas, Controls, Markdown } from '@storybook/addon-docs/blocks';
import * as Stories from './slider.stories';
import body from './slider.md?raw';

<Meta of={Stories} />

# Slider

<Canvas of={Stories.Slider} />
<Controls of={Stories.Slider} />

<Markdown>{body.replace(/^---[\s\S]*?---\s*/, '')}</Markdown>
