import Component from './spacing.stories.twig';

const meta = {
  title: 'Base/Spacing',
  component: Component,
  tags: ['!autodocs'],
};

export default meta;

export const Spacing = {
  parameters: {
    html: {
      disable: true,
    },
  },
};
